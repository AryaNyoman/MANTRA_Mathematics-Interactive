# Arsip kode Manim Community (TIDAK BISA DIJALANKAN)

Berkas di folder ini ditulis untuk Manim Community 0.21. Sejak 2 September 2026
pustaka itu DICABUT dari laptop ARYA (keputusan ARYA: 100% ManimGL), jadi:

- `manimgl` maupun `manim` TIDAK bisa menjalankan berkas di sini.
- JANGAN mencontoh kodenya: nama fungsinya beda (`MathTex` vs `Tex`, `Create` vs
  `ShowCreation`, `config.frame_width` vs `FRAME_WIDTH`, dan seterusnya).
- Video hasilnya (Trigonometri 7, Limit 7, beranda) TETAP tayang di
  `web/public/anim/` dan tidak perlu dirender ulang.

Perkakas yang berlaku sekarang: `manim/gl/`. Contoh rujukan: `manim/contoh/`.
Rancangannya: `docs/superpowers/specs/2026-09-02-pindah-manimgl-dan-standar-ilustrasi-design.md`.
