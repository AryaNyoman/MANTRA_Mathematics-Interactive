/// <reference types="react/canary" />

/*
 * Jenis TypeScript untuk `ViewTransition` dari React. App Router Next 16
 * memakai React canary yang sudah menyediakannya saat dijalankan, tetapi
 * @types/react hanya mengumumkannya lewat rujukan `react/canary` ini.
 * Dipakai oleh components/mantra/Panggung.tsx (pindah halaman berarah).
 */
